[project]
name = "example-uv-project"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests>=2.32"]

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.6"]

[tool.ruff]
line-length = 100
